import { NgModule } from '@angular/core';
import { AuthShellRoutingModule } from '@auth/features/auth-shell/auth-shell-routing.module';

@NgModule( {
               imports: [
                   AuthShellRoutingModule
               ]
           } )
export class AuthShellModule {
}
